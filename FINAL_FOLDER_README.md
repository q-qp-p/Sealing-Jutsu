# Final Generated Research Folder

This is the single final folder containing the project files generated in this chat.

Contents:
- `capsule_guard/` - main Python research prototype code
- `experiments/` - experiment runners
- `tests/` - unit and evidence tests
- `docs/` - research notes, testing guide, readiness queue, and design documents
- `paper/` - current conference draft and reproducibility checklist
- `results/` - CSV, JSONL, and SVG outputs
- `run_capsuleguard.py` - main benchmark entry point
- `run_sensitivity.py` - threshold sensitivity entry point
- `run_llm_experiment.py` - LLM prompt-isolation experiment entry point

Verification command:

```powershell
python -m unittest discover -s tests
```

Quick operation and testing instructions:

```text
TEST_AND_OPERATE_INSTRUCTIONS.md
```
